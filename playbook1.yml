---
- name: Create user on all managed nodes
  hosts: appserver
  tasks:
    - name: Create user
      user:
        name: Radha
        password: intelliqit
        uid: 2222
        home: /tmp/Radha
        shell: /bin/bash
        comment: "A super user"

...
